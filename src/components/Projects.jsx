import { Element } from 'react-scroll'

const Projects = () => {
  return (
    <Element name="projects" id="projects" className="flex items-center justify-center w-full min-h-screen m-auto bg-slate-50">
      <div className="container flex items-center justify-center w-full md:w-[1000px]"></div>
    </Element>
  )
}

export default Projects
